The parser generator is usually ran through its [command-line
interface](../guide/index.html#building-a-grammar), but can also be
invoked as a JavaScript function.

@BuildOptions

@buildParserFile

@buildParser

@GenError
