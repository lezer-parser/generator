program { (Class | Block)* }

Class { "class" "{" "classitem"* "}" }

Block { "{" (Block | "statement")* "}" }

skip { whitespace }

tokens {
  whitespace { std.whitespace+ }
}

---

{ { { class { classitem }

==> Block(Block(Block(⚠),⚠),⚠),Class
