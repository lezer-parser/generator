@top { (class | block)* }

class:class { "class" "{" "classitem"* "}" }

block:block { "{" (block | "statement")* "}" }

@skip { whitespace }

@tokens {
  whitespace { std.whitespace+ }
}

# Can resynchronize to an outer context

{ { { class { classitem }

==> block(block(block(error),error),error),class

# Properly places end-of-file errors

{ { {	

==> block(block(block(error),error),error)
