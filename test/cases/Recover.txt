program { (Class | Block)* }

Class { "class" "{" "classitem"* "}" }

Block { "{" (Block | "statement")* "}" }

tokens {
  skip { std.whitespace* }
}

---

{ { { class { classitem }

==> Block(Block(Block(⚠),⚠),⚠),Class
