prec left binOp { div }

program { expr }

expr { RegExp | Symbol | BinOp }

BinOp { expr !binOp.div "/" expr }

tokens {
  skip { std.whitespace* }

  Symbol { std.asciiLetter+ }

  group {
    RegExp { "/" [^/]+ "/" }
  }
}

---

x / y / /foo/

==> BinOp(BinOp(Symbol,Symbol),RegExp)
