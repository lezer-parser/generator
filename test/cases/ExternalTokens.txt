program { expr* }

expr { tag.Braced<braceOpen expr braceClose> | tag.X<"x" (dot tag.Y<"y">)?> }

external tokens ext1 from "./external_tokens.js" {
  braceOpen,
  braceClose,
  dot=Dot
}

---

x{x}x.y

==> X, Braced(X), X(Dot, Y)
