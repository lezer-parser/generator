@top { expr* }

expr { (braceOpen expr braceClose):braced | ("x" (dot "y":y)?):x }

@external-tokens ext1 from "./external_tokens.js" {
  braceOpen,
  braceClose,
  dot:dot
}

# Uses external tokens

x{x}x.y

==> x, braced(x), x(dot, y)
