program { expr+ }

expr { ArrowExpr | ParenExpr | Identifier }

ParenExpr { conflict.arrow<"(" expr ")"> }

ArrowExpr { conflict.arrow<"(" ParamName ")" "=>" expr> }

tokens {
  skip { std.whitespace* }

  Identifier { std.asciiLetter+ }

  ParamName { std.asciiLetter+ }
}

// FIXME disabled until a better way to specify conflicts is implemented
//! Overlapping tokens

---

(a) => b

==> ArrowExpr(ParamName, Identifier)
