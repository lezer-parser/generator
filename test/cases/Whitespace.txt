top { x+ }

skip { whitespace | comment }

tokens {
  whitespace { (" " | "\n")+ }

  comment:comment { "#" [^\n]* }

  x:x { "x" }
}

# Skips whitespace

x x

==> x, x

# Skips comments

x
x # I'm a comment!
x

==> x, x, comment, x
