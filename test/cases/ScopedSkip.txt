@top { a+ }

@skip { spaces }

@skip { dashes } {
  a:a { "x" | "(" "b" ")" }
}

@tokens {
  spaces { " "+ }
  dashes { "-"+ }
}

# Applies the correct skip rules

x (---b---) (b)

==> a, a, a

# Marks invalid whitespace

( b )

==> a(⚠, ⚠)
